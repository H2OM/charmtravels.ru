import '../tickets.scss';

export default function Tickets() {
    return (
        <section className={"tickets"} id={"tickets"}>
            <div className="container">
                <h2 className={"static__title"}>Билеты</h2>
            </div>
        </section>
    )
}